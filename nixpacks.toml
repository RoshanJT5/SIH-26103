[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.setup]
nixPkgs = ["python312"]

[phases.build]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "python -m alembic upgrade head && python -m uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
